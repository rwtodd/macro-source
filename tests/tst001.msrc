PREFIX: ==
int foo(int a, int b) {
	/* hi there */
	==(do something)
	/* hi there */
	if (a * b)
		==(do something)
	return b;
}

==/do something/
a += b;
a *= b;
==

==/do something()/l
table.insert(result,"b--;")
table.insert(result,"a--;")
==
